{
  "name": "website",
  "version": "1.0.0",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "format": "prettier --write --list-different ."
  },
  "devDependencies": {
    "prettier": "^3.3.1",
    "vitepress": "^1.2.3"
  }
}
